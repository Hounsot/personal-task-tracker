# Personal Task Tracker — Architecture

## ИИ-ассистент

Выбран Yandex AI Studio. UI: `src/Assistant.tsx`; серверный адаптер: `supabase/functions/task-assistant/index.ts`. Модель предлагает одно действие, пользователь подтверждает, приложение применяет его через Supabase с RLS. Функция `task-assistant` развёрнута в Supabase; инструкция и ограничения в `AI_SETUP.md`.

## Актуальное решение — Supabase Cloud

Выбран Supabase Cloud вместо самостоятельного API: Auth, PostgreSQL и Data API с RLS. Реализация подключения находится в `src/api.ts`, вход — в `src/Auth.tsx`, SQL — в `supabase/migrations/`. Инструкция и проверка — `SUPABASE.md`. Для доступа с разных устройств используется один аккаунт. Задачи перечитываются каждые 15 секунд в активном окне и при возвращении фокуса. Создание и изменение подтверждаются сервером. Ниже сохранён первоначальный план; положения о моках и Fastify описывают предыдущий этап.

## Продуктовая аналитика

Подключена Яндекс.Метрика, счетчик `112675328`. Клиентский модуль `src/analytics.ts` загружает счетчик только после явного согласия пользователя в `src/AnalyticsConsent.tsx`. Вебвизор, карта скроллинга и аналитика форм отключены; текст задач, email, пароль, идентификатор пользователя и запросы к ИИ не передаются.

События: `registration_started`, `registration_completed`, `ai_assistant_opened`, `task_created`, `task_completed`. Для `task_completed` передается только числовой параметр `time_to_complete_seconds`: время от первого открытия задачи в браузере либо от создания, если пользователь завершил ее, не открывая. Идентификаторы задач не передаются.

## Цель продукта

Focus — простой личный трекер задач для демонстрации студентам возможностей ИИ-агентов. Первый релиз — интерактивный UI без аккаунтов, сервера и базы данных.

## Пользователь и главный сценарий

Пользователь открывает список задач, создаёт новую через нижнюю форму, видит её в деталях, затем завершает или удаляет её.

## Этапы развития

1. Локальный интерактивный интерфейс (текущий этап).
2. Git/GitHub и проверка на разных устройствах.
3. Fastify API и PostgreSQL в российском облаке.
4. ИИ-функция через изолированный API-адаптер.
5. Публикация, домен, мониторинг и аналитика.

## Технологический стек

- Frontend: React, TypeScript, Vite, CSS и Lucide.
- Backend позже: Node.js, Fastify, TypeScript.
- База позже: PostgreSQL + Prisma.
- Хостинг позже: Timeweb Cloud App Platform и Managed PostgreSQL; резервно — российский VPS с Docker.

## Архитектурная схема

```text
Браузер → React-интерфейс → (позже) Fastify API → PostgreSQL в РФ
```

Задачи хранятся в PostgreSQL Supabase и защищены RLS: пользователь видит и изменяет только свои записи.

## Модель данных Task

- `id`, `title`, `description?`, `project`, `dueDate?`, `priority`, `status`, `createdAt`.

## Экраны и UI-компоненты

- Splash, список задач, детали задачи, нижняя форма создания.
- `TaskRow`, `TaskSection`, `TaskDetails`, `TaskSheet`, базовые кнопки, поля и toast-уведомления.

## Границы текущего этапа

Есть авторизация, Supabase Cloud, синхронизация между устройствами, ИИ-ассистент и продуктовая аналитика. Нет фильтров, поиска, уведомлений, платежей и совместной работы.

## Правила хранения данных и РФ

До появления пользователей персональные данные не хранятся. При добавлении аккаунтов база и API разворачиваются у российского провайдера; отдельно проектируются согласия и политика обработки персональных данных.

## Будущие интеграции: GitHub, БД, ИИ, аналитика

- GitHub — хранение исходного кода и автодеплой.
- PostgreSQL — постоянное хранение задач.
- ИИ — только через отдельный серверный адаптер, позволяющий менять поставщика.
- Аналитика — Яндекс.Метрика с явным согласием и целями ключевых продуктовых сценариев.

## Решения и принятые допущения

- MVP демонстрационный и однопользовательский.
- От 700 CSS px используется desktop-компоновка на всё окно без рамки устройства: верхняя панель с созданием задачи, список слева, детали справа, форма по центру окна. Между 700 и 1050px отступы компактнее, чтобы интерфейс работал и в узкой панели браузера. До 700px сохраняются мобильные переходы и нижняя форма; рабочая область также занимает всё окно. Цвета действий и расположение кнопок завершения и удаления сохраняются из Figma.
- Подтвержденные задачи сохраняются между перезагрузками и устройствами для одного Supabase-аккаунта.

## Чек-лист готовности этапа

- [x] Экраны из Figma перенесены в браузерный интерфейс.
- [x] Есть тестовые задачи и адаптивная вёрстка.
- [x] Проверены mobile (390px), tablet (768px) и desktop (1440px) режимы; форма создания не выходит за границы экрана, а действие создания всегда доступно.
- [x] Работают создание, завершение и удаление задачи.
- [x] Приложение проверено production-сборкой и в браузере.
- [x] Подключены Яндекс.Метрика, согласие пользователя и цели ключевых сценариев.
